@model List<AWWW_lab1_gr1.Models.Student>

<h1>Lista Studentów</h1>

<ul>
    @foreach (var student in Model)
    {
        <li>
            <a asp-controller="Student" asp-action="Details" asp-route-id="@student.Id">@student.FirstName @student.LastName</a>
        </li>
    }
</ul>
