@model IEnumerable<AWWW_lab2_gr1.Models.Tag>

<h2>Tags</h2>

<a asp-action="Create">Dodaj</a>

<table>
    <tr>
        <th>Tag</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
        </tr>
    }
</table>
