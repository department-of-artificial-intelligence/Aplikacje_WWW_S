@model IEnumerable<AWWW_lab3_gr2.Models.Position>

<h1>Lista pozycji</h1>

<div class="w3-card">
    @foreach (var position in Model)
    {
        <div class="w3-container">
            <p>@position.Name</p>
        </div>
    }
</div>
