@model List<Kolokwium.Models.Student>

<h2>Studenci</h2>

<a asp-action="Create">Dodaj studenta</a>

<ul>
    @foreach (var s in Model)
    {
        <li>
            @s.Name (@s.Email)
            <a asp-action="Delete" asp-route-id="@s.Id">Usuń</a>
        </li>
    }
</ul>