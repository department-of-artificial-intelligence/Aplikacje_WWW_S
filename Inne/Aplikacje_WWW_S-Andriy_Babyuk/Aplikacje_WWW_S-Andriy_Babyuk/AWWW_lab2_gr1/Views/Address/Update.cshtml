@model AWWW_lab2_gr1.Models.Address

<div class="container my-5">

    <div class="row justify-content-center">
        <div class="col-md-6">

            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">Update Address</h4>
                </div>

                <div class="card-body">
                    <form method="post" asp-action="UpdateConfirmed">
                        <input type="hidden" name="Id" value="@Model.Id"/>
                        <div class="mb-3">
                            <label asp-for="City" class="form-label"></label>
                            <input asp-for="City" class="form-control" value="@Model.City" required/>
                        </div>
                        <div class="mb-3">
                            <label asp-for="PostalCode" class="form-label"></label>
                            <input asp-for="PostalCode" class="form-control" value="@Model.PostalCode" pattern="^($|^\d{2}-\d{3})$" placeholder="12-345"/>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Street" class="form-label"></label>
                            <input asp-for="Street" class="form-control" value="@Model.Street"/>
                        </div>
                        <div class="d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-secondary">
                                Cancel
                            </a>
                            <button type="submit" class="btn btn-primary">
                                Create
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>